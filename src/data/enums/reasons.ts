import config from "../../config";

/**
 *    An object of bot related reasons.
 */
export const Reasons = {
  ADDED: `Added by ${config.clientName}.`,
  REMOVED: `Removed by ${config.clientName}.`,
  UPDATED: `Updated by ${config.clientName}.`,
};
